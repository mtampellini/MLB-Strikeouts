# Dedicated Odds API key for this repo — must NOT be reused from HR-Picks
# (which sets ODDS_API_KEY). OddsAPIClient enforces this in __init__:
# missing-or-shared key raises CrossRepoKeyBleedError.
# Free tier: 500 requests/month.
ODDS_API_KEY_STRIKEOUTS=
